<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent_Section__c</fullName>
    <label>Parent Section</label>
    <type>Text</type>
    <length>80</length>
    <required>false</required>
    <unique>false</unique>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <description
  >Section Name whose record constrains this one: rows must look up to it, so a key unique only within one parent cannot reach another parent's rows. Leave blank to discover that record from the schema.</description>
</CustomField>
