<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent_Section__c</fullName>
    <label>Parent Section</label>
    <type>Text</type>
    <length>80</length>
    <required>false</required>
    <unique>false</unique>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <description>Repeating only: DeveloperName of the section whose resolved record constrains this section's rows (rows must look up to it). Blank discovers the constraining record from the schema. Text rather than a relationship because custom metadata forbids self-relationships; the loader validates the name and reports a config issue when it is unknown.</description>
</CustomField>
